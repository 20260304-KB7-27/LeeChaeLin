<template>
  <h1>MemberInfo</h1>
  <!-- <div>
    경로 패턴: members/:id <br />
    요청 경로: {{ route.path }} <br />
    전달된 경로: {{ route.params.id }}
  </div> -->

  <div>
    <img :src="member.photo" alt="" />
    <h4>{{ member.name }}({{ member.role }})</h4>
    <p>{{ member.desc }}</p>

    <a
      v-if="member.insta && member.insta !== ''"
      class="fa fa-instagram m-1"
      :href="member.insta"
    ></a>
    <a
      v-if="member.facebook && member.facebook !== ''"
      class="fa fa-facebook m-1"
      :href="member.facebook"
    ></a>
    <a
      v-if="member.youtube && member.youtube !== ''"
      class="fa fa-youtube m-1"
      :href="member.youtube"
    ></a>
  </div>
  <RouterLink to="/members">멤버목록 이동</RouterLink>
</template>

<script setup>
import { RouterLink, useRoute } from 'vue-router';
import members from '@/members.json';

const route = useRoute();

const memberId = Number(route.params.id);

const member = members.find((member) => member.id === memberId);
</script>

<style scoped></style>
